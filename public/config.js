// Runtime configuration placeholder.
// In development this stays empty and the app falls back to VITE_* / .env.
// In the Docker image this file is regenerated at container start from the
// environment (see docker/env.sh).
window.__APP_CONFIG__ = {};
