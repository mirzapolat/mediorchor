// Runtime configuration placeholder.
// In development this stays empty and the app falls back to VITE_* / .env.
// In production the server answers /config.js itself from its environment
// (see server/index.ts), so this file is never served there.
window.__APP_CONFIG__ = {};
